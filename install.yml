version: 1.1
name: hydroviewer_central_america

requirements:
  skip: false
  conda:
    channels:
      - conda-forge
    packages:
      - netCDF4
      - gdal
      - geoglows
      - hydrostats
      - pandas
      - requests
      - plotly
      - numpy
      - scipy
  pip:

post:
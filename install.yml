version: 1.0
name: hydroviewer_central_america

requirements:
  skip: false
  conda:
    channels:
      - conda-forge
    packages:
      - geoglows>=0.18
      - pandas
      - requests
      - scipy
      - plotly

  pip:

post: